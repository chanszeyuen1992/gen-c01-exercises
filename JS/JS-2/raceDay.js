let raceNumber = Math.floor(Math.random() * 1000);
let registeredEarly = false;
let runnerAge = 16;

if (runnerAge >= 18 && registeredEarly === true) {
    raceNumber += 1000;
} else if (runnerAge >= 18 && registeredEarly === flase) {
    raceNumber;
} else {
    raceNumber;
}

console.log(raceNumber);